select * from tab;

select * from bonus;
select * from dept;
select * from emp;
select * from salgrade;


1. 
select * from emp;

select * from EMP
where deptno=(select deptno from emp where ename='BLAKE')

2.
SELECT * FROM EMP
WHERE SAL >= (SELECT AVG(SAL) FROM EMP)
ORDER BY SAL DESC;

3.
SELECT * FROM EMP
WHERE DEPTNO IN
(SELECT DEPTNO FROM EMP WHERE ENAME LIKE '%T%') 

4. 
SELECT * FROM EMP
WHERE DEPTNO = (SELECT DEPTNO FROM DEPT WHERE LOC='DALLAS')

5.
SELECT E.ENAME,D.ENAME FROM EMP E, EMP D
WHERE D.EMPNO=E.MGR
AND D.ENAME='KING';

6.
SELECT * FROM EMP
WHERE DEPTNO = 
(SELECT DEPTNO FROM DEPT WHERE DNAME='SALES') 

7.
SELECT * FROM EMP
WHERE SAL > (SELECT MIN(SAL) FROM EMP WHERE DEPTNO=30);

SELECT * FROM EMP
WHERE SAL >ANY (SELECT SAL FROM EMP WHERE DEPTNO=30);

8.
SELECT * FROM EMP
WHERE JOB IN 
(SELECT DISTINCT JOB FROM EMP WHERE DEPTNO=30) and DEPTNO=10


9.
SELECT * FROM EMP
WHERE (JOB,SAL) IN 
(SELECT JOB,SAL FROM EMP WHERE ENAME='FORD')
AND ENAME <>'FORD'

10.
SELECT * FROM EMP
WHERE JOB =(SELECT JOB FROM EMP WHERE ENAME='JONES')
OR
SAL >= (SELECT SAL FROM EMP WHERE ENAME='FORD')
ORDER BY JOB,SAL DESC

11.
SELECT * FROM EMP
WHERE SAL IN 
(SELECT SAL FROM EMP WHERE ENAME='SMITH' OR ENAME='WARD')
AND ENAME NOT IN('SMITH','WARD')

12.
SELECT * FROM EMP
WHERE JOB IN (SELECT JOB FROM EMP WHERE DEPTNO =
(SELECT DEPTNO FROM DEPT WHERE LOC='CHICAGO'))

13.
SELECT E.* 
FROM EMP E,
(SELECT DEPTNO,AVG(SAL) A_PAY FROM EMP GROUP BY DEPTNO) D
WHERE E.DEPTNO = D.DEPTNO
AND E.SAL > D.A_PAY

14.
SELECT E.DEPTNO,ENAME,SAL
FROM EMP E, 
(SELECT JOB,AVG(SAL) A_PAY FROM EMP GROUP BY JOB) D
WHERE E.JOB=D.JOB
AND E.SAL < D.A_PAY

15.
SELECT DISTINCT D.ENAME,D.JOB,D.DEPTNO,D.EMPNO
FROM EMP E, EMP D
WHERE D.EMPNO=E.MGR


16.
SELECT * FROM EMP
WHERE HIREDATE = (SELECT MAX(HIREDATE) FROM EMP);

