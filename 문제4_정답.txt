--P156 연습문제

1.
SELECT ENAME,P.DEPTNO,DNAME 
FROM EMP P, DEPT D
WHERE P.DEPTNO = D.DEPTNO;

2.
SELECT ENAME,JOB,SAL,DNAME 
FROM EMP E, DEPT D
WHERE E.DEPTNO = D.DEPTNO AND LOC = 'NEW YORK';

3.
SELECT ENAME,DNAME,LOC
FROM EMP E,DEPT D
WHERE E.DEPTNO = D.DEPTNO AND COMM IS NOT NULL;

4.
SELECT ENAME,JOB,DNAME,LOC
FROM EMP E,DEPT D
WHERE E.DEPTNO = D.DEPTNO AND ENAME LIKE '%L%';

5.
SELECT A.ENAME EMPLOYEE,A.EMPNO EMP#, B.ENAME MANAGER,B.EMPNO MGR#
FROM EMP A,EMP B
WHERE A.MGR = B.EMPNO(+);





6.
SELECT A.ENAME, A.HIREDATE,B.ENAME,B.HIREDATE
FROM EMP A,EMP B
WHERE A.MGR = B.EMPNO AND A.HIREDATE > B.HIREDATE;


7. 
SELECT ENAME ||'   '||LPAD('*',CEIL(SAL/100),'*') "Employee and their salary"
FROM EMP

